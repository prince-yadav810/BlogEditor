// Placeholder — AI API client
// Will be implemented in Phase 4 with DeepSeek streaming
// TODO: add streaming fetch call for /api/ai/generate

export async function generateAI(text, action) {
    // POST /api/ai/generate
    // { text, action: 'summarize' | 'fix_grammar' }
    return null
}
